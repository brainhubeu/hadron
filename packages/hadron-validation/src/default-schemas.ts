import email = require('./default-schemas/email.json');
import person = require('./default-schemas/person.json');
import team = require('./default-schemas/team.json');
import user = require('./default-schemas/user.json');
import users = require('./default-schemas/users.json');

const schemas = {
  email,
  person,
  team,
  user,
  users,
};

export default schemas;
