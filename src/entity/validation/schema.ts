import email = require('./schemas/email.json');
import person = require('./schemas/person.json');
import team = require('./schemas/team.json');
import user = require('./schemas/user.json');
import users = require('./schemas/users.json');

const schemas = {
  email,
  person,
  team,
  user,
  users,
};

export default schemas;
